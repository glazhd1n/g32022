#include <bits/stdc++.h>

using namespace std;

int main() {
    string s, s1;
    cin >> s >> s1;

    if(s.find(s1) != -1) {
        cout << s << '\n';
        return;
    } 

    int r = s.size() - 1, r1 = s1.size() - 1;
    for(int i = 0; i < s.size(); i++) {
        
    }
}